@page
@model OdeToFood.Pages.Resaurants.ListModel
@{
}
<h1>RESTAURANTS</h1>

<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" asp-for="SearchItem" class="form-control"/>
            <button class="btn btn-primary">Search</button>
        </div>
    </div>
</form>

<a class="btn btn-primary" asp-page="./Edit">add new restaurant</a>

@*<table class="table">
    <thead>
        <tr>
            <td>Id</td>
            <td>Name</td>
            <td>Location</td>
            <td>Type</td>
            <td>More</td>
            <td>Edit</td>
            <td>Delete</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var restaurant in Model.Restaurants)
        {
        <tr>
            <td>@restaurant.Id</td>
            <td>@restaurant.Name</td>
            <td>@restaurant.Location</td>
            <td>@restaurant.Cuisine</td>
            <td>
                <a asp-page="./Detail" asp-route-restaurantId="@restaurant.Id">more info</a>
            </td>
            <td>
                <a asp-page="./Edit" asp-route-restaurantId="@restaurant.Id">Edit</a>
            </td>
            <td>
                <a asp-page="./Delete" asp-route-restaurantId="@restaurant.Id">Delete</a>
            </td>
        </tr>
        }
    </tbody>

</table>*@

@foreach(var restaurant in Model.Restaurants)
{
    <partial name="_Summary" model="restaurant"/>
}



@section footer {
    <div>Some test content</div>
}
